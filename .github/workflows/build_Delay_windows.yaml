name: Build Delay - Windows
on:
  push:
    branches:
      - master
      - release
    paths:
      - 'plugins/Delay/**'
      - 'modules/**'
      - '.github/workflows/build_Delay_windows.yaml'
jobs:
  build:
    name: Build Delay - Windows
    runs-on: windows-latest
    steps:
      - name: Fix up git URLs
        run: |
          "[url `"https://github.com/`"]`n  insteadOf = `"git@github.com:`"" | Out-File -FilePath $env:USERPROFILE\.gitconfig -Append -Encoding UTF8
        shell: pwsh
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Build plugin
        run: |
          # Build
          cd plugins/Delay
          cmake -B build
          cmake --build build --config Release

          # Package
          New-Item -ItemType Directory -Force -Path "../../ci/bin"
          cd ../../ci/bin
          New-Item -ItemType Directory -Force -Path "vst"
          New-Item -ItemType Directory -Force -Path "vst3"

          Copy-Item "../../plugins/Delay/build/Delay_artefacts/Release/VST/Delay.dll" "vst/"
          Copy-Item "../../plugins/Delay/build/Delay_artefacts/Release/VST3/Delay.vst3" "vst3/" -Recurse

          # Create zip
          7z a Delay_Win.zip vst/Delay.dll vst3/Delay.vst3

          # Upload to socalabs on release branch
          $BRANCH = "${{github.ref}}" -replace 'refs/heads/', ''
          if ($BRANCH -eq "release") {
            curl.exe -F "files=@Delay_Win.zip" "https://socalabs.com/files/set.php?key=$env:APIKEY"
          }
        shell: pwsh
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Delay_Windows
          path: ci/bin/*.zip
          retention-days: 30
