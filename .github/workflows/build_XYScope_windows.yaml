name: Build XYScope - Windows
on:
  push:
    branches:
      - master
    paths:
      - 'plugins/XYScope/**'
jobs:
  build:
    name: Build XYScope - Windows
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Fix up git URLs
        run: |
          "[url `"https://github.com/`"]`n  insteadOf = `"git@github.com:`"" | Out-File -FilePath $env:USERPROFILE\.gitconfig -Append -Encoding UTF8
        shell: pwsh
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Build plugin
        run: |
          # Build
          cd plugins/XYScope
          cmake -B build
          cmake --build build --config Release

          # Package
          New-Item -ItemType Directory -Force -Path "../../ci/bin"
          cd ../../ci/bin
          New-Item -ItemType Directory -Force -Path "vst"
          New-Item -ItemType Directory -Force -Path "vst3"

          Copy-Item "../../plugins/XYScope/build/XYScope_artefacts/Release/VST/XYScope.dll" "vst/"
          Copy-Item "../../plugins/XYScope/build/XYScope_artefacts/Release/VST3/XYScope.vst3" "vst3/" -Recurse

          # Create zip
          7z a XYScope_Win.zip vst/XYScope.dll vst3/XYScope.vst3
        shell: pwsh
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: XYScope_Windows
          path: ci/bin/*.zip
          retention-days: 30
