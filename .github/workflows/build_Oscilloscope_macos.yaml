name: Build Oscilloscope - macOS
on:
  push:
    branches:
      - master
      - release
    paths:
      - 'plugins/Oscilloscope/**'
      - 'modules/**'
      - '.github/workflows/build_Oscilloscope_macos.yaml'
jobs:
  build:
    name: Build Oscilloscope - macOS
    runs-on: macos-latest
    steps:
      - name: Fix up git URLs
        run: |
          echo -e '[url "https://github.com/"]\n  insteadOf = "git@github.com:"' >> ~/.gitconfig
        shell: bash
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Build plugin
        run: |
          # Create a temp keychain
          echo "Create a keychain"
          security create-keychain -p nr4aGPyz Keys.keychain

          echo $APPLICATION | base64 -D -o /tmp/Application.p12
          echo $INSTALLER | base64 -D -o /tmp/Installer.p12

          security import /tmp/Application.p12 -t agg -k Keys.keychain -P aym9PKWB -A -T /usr/bin/codesign
          security import /tmp/Installer.p12 -t agg -k Keys.keychain -P aym9PKWB -A -T /usr/bin/codesign

          security list-keychains -s Keys.keychain
          security default-keychain -s Keys.keychain
          security unlock-keychain -p nr4aGPyz Keys.keychain
          security set-keychain-settings -l -u -t 13600 Keys.keychain
          security set-key-partition-list -S apple-tool:,apple: -s -k nr4aGPyz Keys.keychain

          DEV_APP_ID="Developer ID Application: Roland Rabien (3FS7DJDG38)"

          # Build
          cd plugins/Oscilloscope
          cmake -B build -G Xcode
          cmake --build build --config Release

          # Package
          mkdir -p ../../ci/bin
          cd ../../ci/bin
          mkdir -p au vst vst3

          cp -R "../../plugins/Oscilloscope/build/Oscilloscope_artefacts/Release/AU/Oscilloscope.component" au/
          cp -R "../../plugins/Oscilloscope/build/Oscilloscope_artefacts/Release/VST/Oscilloscope.vst" vst/
          cp -R "../../plugins/Oscilloscope/build/Oscilloscope_artefacts/Release/VST3/Oscilloscope.vst3" vst3/

          # Code sign
          codesign -s "$DEV_APP_ID" -v "vst/Oscilloscope.vst" --options=runtime --timestamp --force
          codesign -s "$DEV_APP_ID" -v "vst3/Oscilloscope.vst3" --options=runtime --timestamp --force
          codesign -s "$DEV_APP_ID" -v "au/Oscilloscope.component" --options=runtime --timestamp --force

          # Notarize
          zip -r Oscilloscope_Mac.zip vst/Oscilloscope.vst vst3/Oscilloscope.vst3 au/Oscilloscope.component

          if [[ -n "$APPLE_USER" ]]; then
            xcrun notarytool submit --verbose --apple-id "$APPLE_USER" --password "$APPLE_PASS" --team-id "3FS7DJDG38" --wait --timeout 30m Oscilloscope_Mac.zip

            rm Oscilloscope_Mac.zip
            xcrun stapler staple vst/Oscilloscope.vst
            xcrun stapler staple vst3/Oscilloscope.vst3
            xcrun stapler staple au/Oscilloscope.component
            zip -r Oscilloscope_Mac.zip vst/Oscilloscope.vst vst3/Oscilloscope.vst3 au/Oscilloscope.component
          fi

          # Upload to socalabs on release branch
          BRANCH="${GITHUB_REF##*/}"
          if [ "$BRANCH" = "release" ]; then
            curl -F "files=@Oscilloscope_Mac.zip" "https://socalabs.com/files/set.php?key=$APIKEY"
          fi
        shell: bash
        env:
          APPLICATION: ${{ secrets.APPLICATION }}
          INSTALLER: ${{ secrets.INSTALLER }}
          APPLE_PASS: ${{ secrets.APPLE_PASS }}
          APPLE_USER: ${{ secrets.APPLE_USER }}
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Oscilloscope_macOS
          path: ci/bin/*.zip
          retention-days: 30
