name: Build PitchTrack - Linux
on:
  push:
    branches:
      - master
    tags:
      - 'pitchtrack_v*'
    paths:
      - 'plugins/PitchTrack/**'
      - 'modules/**'
      - '.github/workflows/build_PitchTrack_linux.yaml'
jobs:
  build:
    name: Build PitchTrack - Linux
    runs-on: ubuntu-latest
    steps:
      - name: Fix up git URLs
        run: |
          echo -e '[url "https://github.com/"]\n  insteadOf = "git@github.com:"' >> ~/.gitconfig
        shell: bash
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install clang ninja-build git ladspa-sdk freeglut3-dev g++ libasound2-dev libcurl4-openssl-dev libfreetype6-dev libjack-jackd2-dev libx11-dev libxcomposite-dev libxcursor-dev libxinerama-dev libxrandr-dev mesa-common-dev webkit2gtk-4.0 juce-tools xvfb
      - name: Build plugin
        run: |
          # Build
          cd plugins/PitchTrack
          cmake -B build -G Ninja -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++
          cmake --build build --config Release

          # Package
          mkdir -p ../../ci/bin
          cd ../../ci/bin
          mkdir -p lv2 vst vst3

          cp -R "../../plugins/PitchTrack/build/PitchTrack_artefacts/LV2/PitchTrack.lv2" lv2/
          cp -R "../../plugins/PitchTrack/build/PitchTrack_artefacts/VST/libPitchTrack.so" "vst/PitchTrack.so"
          cp -R "../../plugins/PitchTrack/build/PitchTrack_artefacts/VST3/PitchTrack.vst3" vst3/

          # Create zip
          zip -r PitchTrack_Linux.zip vst/PitchTrack.so vst3/PitchTrack.vst3 lv2/PitchTrack.lv2

          # Upload to socalabs on release tag
          if [[ "$GITHUB_REF" == refs/tags/pitchtrack_v* ]]; then
            curl -F "files=@PitchTrack_Linux.zip" "https://socalabs.com/files/set.php?key=$APIKEY"
          fi
        shell: bash
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PitchTrack_Linux
          path: ci/bin/*.zip
          retention-days: 30
