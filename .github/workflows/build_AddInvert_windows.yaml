name: Build AddInvert - Windows
on:
  push:
    branches:
      - master
    paths:
      - 'plugins/AddInvert/**'
jobs:
  build:
    name: Build AddInvert - Windows
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Fix up git URLs
        run: |
          "[url `"https://github.com/`"]`n  insteadOf = `"git@github.com:`"" | Out-File -FilePath $env:USERPROFILE\.gitconfig -Append -Encoding UTF8
        shell: pwsh
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Build plugin
        run: |
          # Build
          cd plugins/AddInvert
          cmake -B build
          cmake --build build --config Release

          # Package
          New-Item -ItemType Directory -Force -Path "../../ci/bin"
          cd ../../ci/bin
          New-Item -ItemType Directory -Force -Path "vst"
          New-Item -ItemType Directory -Force -Path "vst3"

          Copy-Item "../../plugins/AddInvert/build/AddInvert_artefacts/Release/VST/AddInvert.dll" "vst/"
          Copy-Item "../../plugins/AddInvert/build/AddInvert_artefacts/Release/VST3/AddInvert.vst3" "vst3/" -Recurse

          # Create zip
          7z a AddInvert_Win.zip vst/AddInvert.dll vst3/AddInvert.vst3
        shell: pwsh
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AddInvert_Windows
          path: ci/bin/*.zip
          retention-days: 30
