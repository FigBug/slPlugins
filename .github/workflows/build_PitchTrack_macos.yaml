name: Build PitchTrack - macOS
on:
  push:
    branches:
      - master
    tags:
      - 'pitchtrack_v*'
    paths:
      - 'plugins/PitchTrack/**'
      - 'modules/**'
      - '.github/workflows/build_PitchTrack_macos.yaml'
jobs:
  build:
    name: Build PitchTrack - macOS
    runs-on: macos-latest
    steps:
      - name: Fix up git URLs
        run: |
          echo -e '[url "https://github.com/"]\n  insteadOf = "git@github.com:"' >> ~/.gitconfig
        shell: bash
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Build plugin
        run: |
          # Create a temp keychain
          echo "Create a keychain"
          security create-keychain -p nr4aGPyz Keys.keychain

          echo $APPLICATION | base64 -D -o /tmp/Application.p12
          echo $INSTALLER | base64 -D -o /tmp/Installer.p12

          security import /tmp/Application.p12 -t agg -k Keys.keychain -P aym9PKWB -A -T /usr/bin/codesign
          security import /tmp/Installer.p12 -t agg -k Keys.keychain -P aym9PKWB -A -T /usr/bin/codesign

          security list-keychains -s Keys.keychain
          security default-keychain -s Keys.keychain
          security unlock-keychain -p nr4aGPyz Keys.keychain
          security set-keychain-settings -l -u -t 13600 Keys.keychain
          security set-key-partition-list -S apple-tool:,apple: -s -k nr4aGPyz Keys.keychain

          DEV_APP_ID="Developer ID Application: Roland Rabien (3FS7DJDG38)"

          # Build
          cd plugins/PitchTrack
          cmake -B build -G Xcode
          cmake --build build --config Release

          # Package
          mkdir -p ../../ci/bin
          cd ../../ci/bin
          mkdir -p au vst vst3

          cp -R "../../plugins/PitchTrack/build/PitchTrack_artefacts/Release/AU/PitchTrack.component" au/
          cp -R "../../plugins/PitchTrack/build/PitchTrack_artefacts/Release/VST/PitchTrack.vst" vst/
          cp -R "../../plugins/PitchTrack/build/PitchTrack_artefacts/Release/VST3/PitchTrack.vst3" vst3/

          # Code sign
          codesign -s "$DEV_APP_ID" -v "vst/PitchTrack.vst" --options=runtime --timestamp --force
          codesign -s "$DEV_APP_ID" -v "vst3/PitchTrack.vst3" --options=runtime --timestamp --force
          codesign -s "$DEV_APP_ID" -v "au/PitchTrack.component" --options=runtime --timestamp --force

          # Notarize
          zip -r PitchTrack_Mac.zip vst/PitchTrack.vst vst3/PitchTrack.vst3 au/PitchTrack.component

          if [[ -n "$APPLE_USER" ]]; then
            xcrun notarytool submit --verbose --apple-id "$APPLE_USER" --password "$APPLE_PASS" --team-id "3FS7DJDG38" --wait --timeout 30m PitchTrack_Mac.zip

            rm PitchTrack_Mac.zip
            xcrun stapler staple vst/PitchTrack.vst
            xcrun stapler staple vst3/PitchTrack.vst3
            xcrun stapler staple au/PitchTrack.component
            zip -r PitchTrack_Mac.zip vst/PitchTrack.vst vst3/PitchTrack.vst3 au/PitchTrack.component
          fi

          # Upload to socalabs on release tag
          if [[ "$GITHUB_REF" == refs/tags/pitchtrack_v* ]]; then
            curl -F "files=@PitchTrack_Mac.zip" "https://socalabs.com/files/set.php?key=$APIKEY"
          fi
        shell: bash
        env:
          APPLICATION: ${{ secrets.APPLICATION }}
          INSTALLER: ${{ secrets.INSTALLER }}
          APPLE_PASS: ${{ secrets.APPLE_PASS }}
          APPLE_USER: ${{ secrets.APPLE_USER }}
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: PitchTrack_macOS
          path: ci/bin/*.zip
          retention-days: 30
