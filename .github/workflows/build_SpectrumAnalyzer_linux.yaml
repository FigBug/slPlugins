name: Build SpectrumAnalyzer - Linux
on:
  push:
    branches:
      - master
      - release
    paths:
      - 'plugins/SpectrumAnalyzer/**'
      - 'modules/**'
      - '.github/workflows/build_SpectrumAnalyzer_linux.yaml'
jobs:
  build:
    name: Build SpectrumAnalyzer - Linux
    runs-on: ubuntu-latest
    steps:
      - name: Fix up git URLs
        run: echo -e '[url "https://github.com/"]
  insteadOf = "git@github.com:"' >> ~/.gitconfig
        shell: bash
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install clang ninja-build git ladspa-sdk freeglut3-dev g++ libasound2-dev libcurl4-openssl-dev libfreetype6-dev libjack-jackd2-dev libx11-dev libxcomposite-dev libxcursor-dev libxinerama-dev libxrandr-dev mesa-common-dev webkit2gtk-4.0 juce-tools xvfb
      - name: Build plugin
        run: |
          # Build
          cd plugins/SpectrumAnalyzer
          cmake -B build -G Ninja -DCMAKE_C_COMPILER=clang -DCMAKE_CXX_COMPILER=clang++
          cmake --build build --config Release

          # Package
          mkdir -p ../../ci/bin
          cd ../../ci/bin
          mkdir -p lv2 vst vst3

          cp -R "../../plugins/SpectrumAnalyzer/build/SpectrumAnalyzer_artefacts/Release/LV2/SpectrumAnalyzer.lv2" lv2/
          cp -R "../../plugins/SpectrumAnalyzer/build/SpectrumAnalyzer_artefacts/Release/VST/libSpectrumAnalyzer.so" "vst/SpectrumAnalyzer.so"
          cp -R "../../plugins/SpectrumAnalyzer/build/SpectrumAnalyzer_artefacts/Release/VST3/SpectrumAnalyzer.vst3" vst3/

          # Create zip
          zip -r SpectrumAnalyzer_Linux.zip vst/SpectrumAnalyzer.so vst3/SpectrumAnalyzer.vst3 lv2/SpectrumAnalyzer.lv2

          # Upload to socalabs on release branch
          BRANCH=${{GITHUB_REF##*/}}
          if [ "$BRANCH" = "release" ]; then
            curl -F "files=@SpectrumAnalyzer_Linux.zip" "https://socalabs.com/files/set.php?key=$APIKEY"
          fi
        shell: bash
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: SpectrumAnalyzer_Linux
          path: ci/bin/*.zip
          retention-days: 30
