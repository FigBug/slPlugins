name: Release Oscilloscope - Windows
on:
  push:
    tags:
      - 'oscilloscope_v*'
jobs:
  build:
    name: Release Oscilloscope - Windows
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Fix up git URLs
        run: |
          "[url `"https://github.com/`"]`n  insteadOf = `"git@github.com:`"" | Out-File -FilePath $env:USERPROFILE\.gitconfig -Append -Encoding UTF8
        shell: pwsh
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Build plugin
        run: |
          # Build
          cd plugins/Oscilloscope
          cmake -B build
          cmake --build build --config Release

          # Package
          New-Item -ItemType Directory -Force -Path "../../ci/bin"
          cd ../../ci/bin
          New-Item -ItemType Directory -Force -Path "vst"
          New-Item -ItemType Directory -Force -Path "vst3"

          Copy-Item "../../plugins/Oscilloscope/build/Oscilloscope_artefacts/Release/VST/Oscilloscope.dll" "vst/"
          Copy-Item "../../plugins/Oscilloscope/build/Oscilloscope_artefacts/Release/VST3/Oscilloscope.vst3" "vst3/" -Recurse

          # Create zip
          7z a Oscilloscope_Win.zip vst/Oscilloscope.dll vst3/Oscilloscope.vst3

          # Upload to socalabs on release tag
          if ("${{github.ref}}" -like "refs/tags/oscilloscope_v*") {
            curl.exe -F "files=@Oscilloscope_Win.zip" "https://socalabs.com/files/set.php?key=$env:APIKEY"
          }
        shell: pwsh
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Oscilloscope_Windows
          path: ci/bin/*.zip
          retention-days: 30
      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: ci/bin/*.zip
