name: Build Expander - Windows
on:
  push:
    branches:
      - master
    paths:
      - 'plugins/Expander/**'
jobs:
  build:
    name: Build Expander - Windows
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - name: Fix up git URLs
        run: |
          "[url `"https://github.com/`"]`n  insteadOf = `"git@github.com:`"" | Out-File -FilePath $env:USERPROFILE\.gitconfig -Append -Encoding UTF8
        shell: pwsh
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Build plugin
        run: |
          # Build
          cd plugins/Expander
          cmake -B build
          cmake --build build --config Release

          # Package
          New-Item -ItemType Directory -Force -Path "../../ci/bin"
          cd ../../ci/bin
          New-Item -ItemType Directory -Force -Path "vst"
          New-Item -ItemType Directory -Force -Path "vst3"

          Copy-Item "../../plugins/Expander/build/Expander_artefacts/Release/VST/Expander.dll" "vst/"
          Copy-Item "../../plugins/Expander/build/Expander_artefacts/Release/VST3/Expander.vst3" "vst3/" -Recurse

          # Create zip
          7z a Expander_Win.zip vst/Expander.dll vst3/Expander.vst3
        shell: pwsh
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Expander_Windows
          path: ci/bin/*.zip
          retention-days: 30
