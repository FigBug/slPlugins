name: Release CompensatedDelay - macOS
on:
  push:
    tags:
      - 'compensateddelay_v*'
jobs:
  build:
    name: Release CompensatedDelay - macOS
    runs-on: macos-latest
    timeout-minutes: 30
    steps:
      - name: Fix up git URLs
        run: |
          echo -e '[url "https://github.com/"]\n  insteadOf = "git@github.com:"' >> ~/.gitconfig
        shell: bash
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Build plugin
        run: |
          # Create a temp keychain
          echo "Create a keychain"
          security create-keychain -p nr4aGPyz Keys.keychain

          echo $APPLICATION | base64 -D -o /tmp/Application.p12
          echo $INSTALLER | base64 -D -o /tmp/Installer.p12

          security import /tmp/Application.p12 -t agg -k Keys.keychain -P aym9PKWB -A -T /usr/bin/codesign
          security import /tmp/Installer.p12 -t agg -k Keys.keychain -P aym9PKWB -A -T /usr/bin/codesign

          security list-keychains -s Keys.keychain
          security default-keychain -s Keys.keychain
          security unlock-keychain -p nr4aGPyz Keys.keychain
          security set-keychain-settings -l -u -t 13600 Keys.keychain
          security set-key-partition-list -S apple-tool:,apple: -s -k nr4aGPyz Keys.keychain

          DEV_APP_ID="Developer ID Application: Roland Rabien (3FS7DJDG38)"

          # Build
          cd plugins/CompensatedDelay
          cmake -B build -G Xcode
          cmake --build build --config Release

          # Package
          mkdir -p ../../ci/bin
          cd ../../ci/bin
          mkdir -p au vst vst3

          cp -R "../../plugins/CompensatedDelay/build/CompensatedDelay_artefacts/Release/AU/CompensatedDelay.component" au/
          cp -R "../../plugins/CompensatedDelay/build/CompensatedDelay_artefacts/Release/VST/CompensatedDelay.vst" vst/
          cp -R "../../plugins/CompensatedDelay/build/CompensatedDelay_artefacts/Release/VST3/CompensatedDelay.vst3" vst3/

          # Code sign
          codesign -s "$DEV_APP_ID" -v "vst/CompensatedDelay.vst" --options=runtime --timestamp --force
          codesign -s "$DEV_APP_ID" -v "vst3/CompensatedDelay.vst3" --options=runtime --timestamp --force
          codesign -s "$DEV_APP_ID" -v "au/CompensatedDelay.component" --options=runtime --timestamp --force

          # Notarize
          zip -r CompensatedDelay_Mac.zip vst/CompensatedDelay.vst vst3/CompensatedDelay.vst3 au/CompensatedDelay.component

          if [[ -n "$APPLE_USER" ]]; then
            xcrun notarytool submit --verbose --apple-id "$APPLE_USER" --password "$APPLE_PASS" --team-id "3FS7DJDG38" --wait --timeout 30m CompensatedDelay_Mac.zip

            rm CompensatedDelay_Mac.zip
            xcrun stapler staple vst/CompensatedDelay.vst
            xcrun stapler staple vst3/CompensatedDelay.vst3
            xcrun stapler staple au/CompensatedDelay.component
            zip -r CompensatedDelay_Mac.zip vst/CompensatedDelay.vst vst3/CompensatedDelay.vst3 au/CompensatedDelay.component
          fi

          # Upload to socalabs on release tag
          if [[ "$GITHUB_REF" == refs/tags/compensateddelay_v* ]]; then
            curl -F "files=@CompensatedDelay_Mac.zip" "https://socalabs.com/files/set.php?key=$APIKEY"
          fi
        shell: bash
        env:
          APPLICATION: ${{ secrets.APPLICATION }}
          INSTALLER: ${{ secrets.INSTALLER }}
          APPLE_PASS: ${{ secrets.APPLE_PASS }}
          APPLE_USER: ${{ secrets.APPLE_USER }}
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: CompensatedDelay_macOS
          path: ci/bin/*.zip
          retention-days: 30
      - name: Extract Changelog
        run: |
          VERSION="${GITHUB_REF_NAME#compensateddelay_v}"
          awk -v ver="$VERSION" '
            BEGIN { found=0; printing=0; pattern="^"ver":?$" }
            $0 ~ pattern { found=1; printing=1; next }
            printing && /^[0-9]+\.[0-9]+\.[0-9]+:?$/ { printing=0 }
            printing { print }
          ' ./plugins/CompensatedDelay/Changelist.txt > /tmp/release_notes.txt
        shell: bash
      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          files: ci/bin/*.zip
          body_path: /tmp/release_notes.txt
