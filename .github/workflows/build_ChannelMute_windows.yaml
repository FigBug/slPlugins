name: Build ChannelMute - Windows
on:
  push:
    branches:
      - master
    tags:
      - 'channelmute_v*'
    paths:
      - 'plugins/ChannelMute/**'
      - 'modules/**'
      - '.github/workflows/build_ChannelMute_windows.yaml'
jobs:
  build:
    name: Build ChannelMute - Windows
    runs-on: windows-latest
    steps:
      - name: Fix up git URLs
        run: |
          "[url `"https://github.com/`"]`n  insteadOf = `"git@github.com:`"" | Out-File -FilePath $env:USERPROFILE\.gitconfig -Append -Encoding UTF8
        shell: pwsh
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          submodules: recursive
      - name: Build plugin
        run: |
          # Build
          cd plugins/ChannelMute
          cmake -B build
          cmake --build build --config Release

          # Package
          New-Item -ItemType Directory -Force -Path "../../ci/bin"
          cd ../../ci/bin
          New-Item -ItemType Directory -Force -Path "vst"
          New-Item -ItemType Directory -Force -Path "vst3"

          Copy-Item "../../plugins/ChannelMute/build/ChannelMute_artefacts/Release/VST/ChannelMute.dll" "vst/"
          Copy-Item "../../plugins/ChannelMute/build/ChannelMute_artefacts/Release/VST3/ChannelMute.vst3" "vst3/" -Recurse

          # Create zip
          7z a ChannelMute_Win.zip vst/ChannelMute.dll vst3/ChannelMute.vst3

          # Upload to socalabs on release tag
          if ("${{github.ref}}" -like "refs/tags/channelmute_v*") {
            curl.exe -F "files=@ChannelMute_Win.zip" "https://socalabs.com/files/set.php?key=$env:APIKEY"
          }
        shell: pwsh
        env:
          APIKEY: ${{ secrets.APIKEY }}
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ChannelMute_Windows
          path: ci/bin/*.zip
          retention-days: 30
